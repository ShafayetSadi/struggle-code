# Prompts

System prompts for the Struggle AI core will live in this folder as Markdown files and be loaded at runtime by the core package.

Prompt content is intentionally omitted from this scaffold so the prompt layer can evolve independently from the public API.

